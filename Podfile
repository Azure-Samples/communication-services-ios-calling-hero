platform :ios, '14.0'
use_frameworks!

target 'AzureCalling' do

pod 'AzureCommunicationCalling', '2.2.0'
pod 'AzureCommunicationUICalling', '1.0.0'
pod 'MicrosoftFluentUI' #, '0.5.2' needs UI library update, so we let cocoapods use the version in the UI calling library
pod 'AzureCore', '1.0.0-beta.12'
pod 'MSAL', '1.2.2'
pod 'SwiftLint', '0.42.0'

end
